
namespace GameState {
    /// <summary>
    /// A mutable (for simplicity) game context that is passed around states.
    /// This context can contain things like: Score, number of playthroughs, etc..
    /// It is  
    /// </summary>
    public class GameContext {
    }
}